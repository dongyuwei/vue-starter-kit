export default (a, b) => {
  // for (let index = 0; index < 10; index++) {
  //   debugger;
  // }
  // throw new Error("oops");
  return a + b;
};

export const foo2 = `foo foo`;
export const data = { [foo2]: "bar" };
